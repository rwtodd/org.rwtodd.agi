plugins {
    // Apply the java-library plugin to add support for Java Library
    id 'java-library'
}

archivesBaseName = 'org.rwtodd.agi'

dependencies {
        testImplementation(platform('org.junit:junit-bom:5.9.1'))
        testImplementation('org.junit.jupiter:junit-jupiter')
}

